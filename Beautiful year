#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;
    for(int i=n+1; i<=9012; i++){
        int a=i/1000;
        int b=(i/100)%10;
        int c=(i/10)%10;
        int d=i%10;
        if(a!=b && b!=c &&c!=d && a!=c && a!=d && b!=d){
            cout << i;
            return 0;
        }
    }
}
