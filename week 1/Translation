#include <bits/stdc++.h>
using namespace std;

int main(){
    string s,t;
    cin >> s >> t;
    
    for(int i=0; i<s.size()/2; i++){
        char a = s[i];
        s[i] = s[s.size()-1-i];
        s[s.size()-1-i] = a;
    }
    if(s==t) cout << "YES";
    else cout << "NO";
}
