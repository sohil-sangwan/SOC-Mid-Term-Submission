#include <bits/stdc++.h>
using namespace std;

int main() {
    long long cnt1, cnt2, cnt3, cnt4;
    cin >> cnt1 >> cnt2 >> cnt3 >> cnt4;

    if (cnt1 != cnt4) {
        cout << 0;
    }
    else {
        // if no "((" exists, we cannot place any ")("
        if (cnt1 == 0 && cnt3 > 0)
            cout << 0;
        else
            cout << 1;
    }

    return 0;
}
